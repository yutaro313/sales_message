<div class="post-detail py-5 bg-light">
  <div class="card w-75 mx-auto mt-4 shadow-lg">
    <%= attachment_image_tag @post, :post_image, class:"card-img-top"; %>
    <div class="card-body">
      <div class="row">
        <div class="col-2">
          <%#= attachment_image_tag @post.sale, :profile_image, :fill, 60, 60, fallback: "no_image.jpg", class:"mt-3 rounded-circle"; %>
        </div>
        <div class="col-10">
          <p class="card-title">
              <strong><i class="far fa-flag"></i><span> <%= @post.post_title %></span></strong>
          </p>
          <p class="card-title">説明：<%= @post.introduction %></p>
          <span><i class="fas fa-user"></i> By <%= @sale.name %> ｜ </span>
          <span><i class="fa fa-calendar"></i> <%= @post.created_at.strftime('%Y/%m/%d') %> ｜ </span>
          <span><i class="fa fa-comment"></i> <%#= link_to "#{@post.post_comments.count}", post_image_path(@post_image.id), class:"text-dark" %> ｜ </span>
          <!--<%# if @post_image.favorited_by?(current_user) %>-->
          <!--  <span>-->
          <!--    <%#= link_to(post_image_favorites_path(@post_image), class:"text-dark", method: :delete) do %>-->
          <!--      <i class="fa fa-heart" aria-hidden="true"></i> <%#= @post_image.favorites.count %>-->
          <!--    <%# end %>-->
          <!--  </span>-->
          <!--  <%# else %>-->
          <!--  <span>-->
          <!--    <%#= link_to(post_image_favorites_path(@post_image), class:"text-dark", method: :post) do %>-->
          <!--      <i class="fa fa-heart" aria-hidden="true"></i> <%#= @post_image.favorites.count %>-->
          <!--    <%# end %>-->
          <!--  </span>-->
          <!--<%# end %>-->
          <% if @post.sale == current_sale %>
            <span> ｜ </span><%= link_to "削除",sale_post_path(@post), class:"text-dark", method: :delete %>
          <% end %>
        </div>
      </div>
      <hr>
      <div class="comments card-text">
        <h4 class="mt-3"><%= @post.post_comments.count %>件コメント</h4>
        <% @post.post_comments.each do |post_comment| %>
          <div class="media border mb-3">
            <%#= attachment_image_tag @post, :profile_image, :fill, 60, 60, fallback: "no_image.jpg", class:"bd-placeholder-img align-self-center mx-3" %>
            <%= post_user_name(post_comment.post_user_id, post_comment.action) %>
            <div class="media-body ml-4 p-3">
              <p><%= post_comment.created_at.strftime('%Y/%m/%d') %><%= post_comment.comment %></p>
              <p><%= post_comment.comment %></p>
              <% if @post.sale == current_sale %>
               <%= link_to "削除", sale_post_path(@post), method: :delete, class: "btn btn-danger pull-right" %>
              <% end %>
            </div>
          </div>
        <% end %>
      </div>
      <hr>
      <div class="new-comment card-text">
        <%= form_with(model:[@post, @post_comment], url: sale_post_post_comments_path(@post.id), local:true) do |f| %>
          <div class="form-group">
            <label for="exampleFormControlTextarea1">コメントする</label>
            <%= f.text_area :comment, rows:'5', class: "form-control",placeholder: "コメントをここに" %>
          </div>
          <%= f.submit "送信する", class:"btn btn-lg btn-info mt-2 float-right" %>
        <% end %>
      </div>
    </div>
  </div>
</div>
